# API Keys — NIEMALS committen
.env
.env.*

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Datenbank (zu gross für Git)
data/*.db
data/raw/*.json
data/raw/*.csv
data/literature/raw/

# IDE
.vscode/settings.json
.idea/

# Claude Code
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# Logs
logs/*.jsonl
logs/*.log

# Reports (regenerierbar)
reports/*.md
data/results/monitor_mcp_audit_log.jsonl

# Generated agent review-queue runtime log (regenerated each run)
data/results/agent_review_queue_llm_audit_log.jsonl
# MS Office lock/temp files
~$*

# Private Live-Bot-Daten (nicht thesis-facing)
data/live/

# Taeglicher Publish-Output (regenerierbar via daily_review_run)
data/publish/
data/results/daily_review_llm_audit_log.jsonl

# Tageslauf-Arbeitskopien der Kurations-Dateien (gefiltert, regenerierbar)
data/results/daily_review_filtered_status_updates.csv
data/results/daily_review_filtered_decisions.csv

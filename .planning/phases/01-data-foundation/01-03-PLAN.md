---
phase: 01-data-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
files_modified:
  - ingest/fivethirtyeight.py
  - ingest/rcp.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "poll_forecasts contains daily rows for source='fivethirtyeight' covering Jan-Nov 2024"
    - "poll_forecasts contains rows for source='rcp' with probability in [0.0, 1.0]"
    - "RCP probability > 0.5 when Trump poll average exceeds Harris poll average"
    - "FiveThirtyEight rows have probability derived from pct_estimate (not raw percentage > 1.0)"
    - "Both scripts are idempotent: running twice does not create duplicate rows"
  artifacts:
    - path: "ingest/fivethirtyeight.py"
      provides: "538 CSV parse and write to poll_forecasts"
      exports:
        - parse_csv
        - ingest_fivethirtyeight
    - path: "ingest/rcp.py"
      provides: "RCP scrape and logit-to-probability conversion"
      exports:
        - poll_pct_to_probability
        - ingest_rcp
  key_links:
    - from: "ingest/fivethirtyeight.py"
      to: "data/thesis.db:poll_forecasts"
      via: "INSERT OR IGNORE with source='fivethirtyeight'"
      pattern: "INSERT OR IGNORE INTO poll_forecasts"
    - from: "ingest/rcp.py"
      to: "scipy.special.expit"
      via: "poll_pct_to_probability() logit conversion"
      pattern: "expit.*margin"
---

<objective>
Build FiveThirtyEight CSV ingestion and RCP poll scraping scripts, both writing to
poll_forecasts with candidate probabilities in [0.0, 1.0].

Purpose: DATA-03 and DATA-04 — both poll sources must be in poll_forecasts before
Brier Score computation (Phase 3) can compare them against Polymarket prices.

Output: ingest/fivethirtyeight.py and ingest/rcp.py. FiveThirtyEight reads from
the GitHub CSV. RCP uses the realclearpolitics PyPI package with logit conversion
via scipy.special.expit.
</objective>

<execution_context>
@C:/Users/chole/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chole/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@ingest/__init__.py
@requirements.txt

<interfaces>
<!-- poll_forecasts table (correct schema from 01-01): -->
<!--
INSERT OR IGNORE INTO poll_forecasts
    (date, source, candidate, probability, poll_type)
VALUES (?, ?, ?, ?, ?)

UNIQUE(date, source, candidate, poll_type)
probability: REAL in [0.0, 1.0]
source: 'fivethirtyeight' or 'rcp'
candidate: 'trump' or 'harris'
poll_type: 'model' for 538, 'rcp_converted' for RCP
-->

<!-- FiveThirtyEight data (MEDIUM confidence from RESEARCH.md): -->
<!--
GitHub URL: https://raw.githubusercontent.com/fivethirtyeight/data/master/polls/2024-averages/presidential_general_averages_2024-09-12_uncorrected.csv
Columns (need verification at runtime): date, candidate, pct_estimate, ... (possibly more)
PITFALL: CSV may only cover through Sep 12, 2024 — check MIN/MAX date and log warning
FALLBACK: If dates don't cover Jan-Nov 2024 fully, use state-of-the-polls-2024 directory data
-->

<!-- RCP conversion (from RESEARCH.md): -->
<!--
from scipy.special import expit
margin = (trump_pct - harris_pct) / 100.0
probability = float(expit(margin * 4.0))
Scaling factor 4.0 is a methodological choice — must be stored as metadata
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ingest/fivethirtyeight.py — CSV parse and write</name>
  <files>ingest/fivethirtyeight.py</files>
  <behavior>
    - parse_csv(csv_text: str) -> list[dict]:
      * Reads CSV using pandas.read_csv(io.StringIO(csv_text))
      * Detects which column holds the forecast probability: looks for 'pct_estimate', 'mean', 'avg' in columns
      * Normalizes probability: if column value > 1.0, divides by 100.0 (handles both 0.52 and 52.3 formats)
      * Returns list of dicts with keys: date (YYYY-MM-DD str), source ('fivethirtyeight'), candidate (lowercase), probability (float in [0.0, 1.0]), poll_type ('model')
      * Filters to only Trump and Harris rows; skips third-party candidates
      * Validates: assert all(0.0 <= row["probability"] <= 1.0 for row in result)
    - ingest_fivethirtyeight(db_path: Path = DB_PATH) -> int:
      * Downloads CSV from https://raw.githubusercontent.com/fivethirtyeight/data/master/polls/2024-averages/presidential_general_averages_2024-09-12_uncorrected.csv
      * Logs date range coverage: MIN and MAX date in CSV
      * If MIN date > 2024-02-01: logs WARNING "FiveThirtyEight CSV partial coverage — starting {min_date}"
      * Writes to poll_forecasts via INSERT OR IGNORE
      * Returns row count
    - All functions: type hints, German docstrings, no module-level side effects
  </behavior>
  <action>
    Create ingest/fivethirtyeight.py.
    Imports: pandas as pd, io, httpx, asyncio, sqlite3, pathlib, from ingest import get_connection, DB_PATH

    Key implementation notes:
    - Use pandas for CSV parsing (handles encoding, NA, date normalization automatically per RESEARCH.md)
    - Column detection: try each candidate column name in priority order ['pct_estimate', 'mean', 'avg', 'value']
    - The CSV candidate column may be named 'candidate' or 'name' — handle both
    - Trump detection: candidate.lower() in ['donald trump', 'trump', 'd. trump']
    - Harris detection: candidate.lower() in ['kamala harris', 'harris', 'k. harris']
    - Use httpx.get() (sync, not async) for simplicity — this is a one-time download

    if __name__ == "__main__": ingest_fivethirtyeight() wrapped in asyncio.run() is NOT needed
    (sync script). Just call directly.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_fivethirtyeight_rows -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_fivethirtyeight_rows passes.
    `python -c "from ingest.fivethirtyeight import parse_csv"` exits 0.
    Running `python ingest/fivethirtyeight.py` prints row count and date range.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest/rcp.py — scrape and logit conversion</name>
  <files>ingest/rcp.py</files>
  <behavior>
    - poll_pct_to_probability(trump_pct: float, harris_pct: float, scaling_factor: float = 4.0) -> float:
      * Uses scipy.special.expit(margin * scaling_factor)
      * margin = (trump_pct - harris_pct) / 100.0
      * Raises ValueError if either pct is <= 0.0
      * Result is strictly in (0.0, 1.0) due to expit properties
      * This is the pure function tested by test_rcp_probability_range and test_rcp_symmetric
    - ingest_rcp(db_path: Path = DB_PATH) -> int:
      * Primary method: try importing realclearpolitics package, call its API for RCP general election 2024 poll averages
      * If realclearpolitics import fails or scrape fails: fall back to a note in logs and return 0
      * For each daily poll average row: call poll_pct_to_probability(trump_avg, harris_avg)
      * Insert into poll_forecasts with source='rcp', candidate='trump', probability from conversion, poll_type='rcp_converted'
      * Also insert harris row with probability = 1.0 - trump_probability (two candidates, one market)
      * INSERT OR IGNORE for idempotency
      * Returns rows inserted
    - Scaling factor (4.0) is documented in a module-level constant SCALING_FACTOR with a comment
      explaining it is a methodological choice documented in the thesis methodology section
    - All functions: type hints, German docstrings
  </behavior>
  <action>
    Create ingest/rcp.py.
    Imports: from scipy.special import expit, import pandas as pd, sqlite3, pathlib, logging
    from ingest import get_connection, DB_PATH

    SCALING_FACTOR: float = 4.0  # Methodological choice — see thesis Section 3.2

    Implement poll_pct_to_probability() with the expit formula. This is the most
    tested function in the file — it must be importable without scipy failing.

    For ingest_rcp(), use try/except around the realclearpolitics import:
      try:
          import realclearpolitics
          # ... use package
      except ImportError:
          logging.warning("realclearpolitics package not installed. Run: pip install realclearpolitics")
          # ... try direct HTTP fallback or return 0

    RCP URL for the 2024 general election average:
    "https://www.realclearpolling.com/polls/president/general/2024/trump-vs-harris"
    (Note: URL may have changed from realclearpolitics.com to realclearpolling.com — handle both)

    PITFALL 4 mitigation: assert all inserted probabilities are in [0.0, 1.0] before commit.

    if __name__ == "__main__": ingest_rcp() with print of result count.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_rcp_probability_range tests/test_ingest.py::test_rcp_symmetric -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_rcp_probability_range passes: poll_pct_to_probability(52.0, 47.0) returns a float in (0.5, 1.0).
    test_rcp_symmetric passes: poll_pct_to_probability(50.0, 50.0) == pytest.approx(0.5, abs=0.01).
    `python -c "from ingest.rcp import poll_pct_to_probability; print(poll_pct_to_probability(52.0, 47.0))"` prints a value between 0.5 and 1.0.
  </done>
</task>

</tasks>

<verification>
Unit tests pass:
  .venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_fivethirtyeight_rows tests/test_ingest.py::test_rcp_probability_range tests/test_ingest.py::test_rcp_symmetric -v

Data integrity checks (after scripts run):
  .venv/Scripts/python.exe -c "
  import sqlite3; c = sqlite3.connect('data/thesis.db')
  r = c.execute(\"SELECT source, COUNT(*), MIN(date), MAX(date) FROM poll_forecasts GROUP BY source\").fetchall()
  for row in r: print(row)
  bad = c.execute(\"SELECT COUNT(*) FROM poll_forecasts WHERE probability < 0 OR probability > 1\").fetchone()[0]
  print(f'Probability range violations: {bad} (must be 0)')
  "

RCP conversion validation:
  .venv/Scripts/python.exe -c "
  import sqlite3; c = sqlite3.connect('data/thesis.db')
  print(c.execute(\"SELECT MIN(probability), MAX(probability) FROM poll_forecasts WHERE source='rcp'\").fetchone())
  "
</verification>

<success_criteria>
- test_fivethirtyeight_rows, test_rcp_probability_range, test_rcp_symmetric all pass
- poll_forecasts has rows for source='fivethirtyeight' with daily coverage (check MIN/MAX date)
- poll_forecasts has rows for source='rcp' with all probability values in (0.0, 1.0)
- No probability values outside [0.0, 1.0] in poll_forecasts for either source
- Both scripts are idempotent (second run does not increase row count)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>

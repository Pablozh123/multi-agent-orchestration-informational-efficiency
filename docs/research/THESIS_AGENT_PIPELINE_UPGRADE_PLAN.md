# Thesis Agent Pipeline Upgrade Plan

Dieses Dokument beschreibt nur, wie die Pipeline spaeter mit Assistenz-Agenten verbessert werden koennte. Es implementiert, aktiviert und nutzt keine Runtime-Agenten, kein MCP, kein Model Routing und keine LLM-Metriken.

## Counts

- Upgrade rows: 7
- Documentation-only rows: 6
- Deferred rows: 1

## Upgrade Sequence

| upgrade_id | future_assistance_role | sequence_after_core_de | current_status | next_safe_step_de |
| --- | --- | --- | --- | --- |
| agent_upgrade_01 | Manual source review | Nach H1-H2-H3-Kern: fehlende Page-/Section-Notes und Claim-Support-Felder vorbereiten. | future_documentation_only | Nach manueller Quellenreview ein separates Prompt-/Audit-Design fuer fehlende Page Notes schreiben. |
| agent_upgrade_02 | Evidence-to-prose drafting | Nach stabiler Core-Section-CSV: Evidence-IDs in kurze Entwurfsnotizen uebersetzen. | future_documentation_only | Nach stabiler Traceability kurze evidence_id-bezogene Draft-Notes spezifizieren. |
| agent_upgrade_03 | Claim and wording review | Nach erstem Kapiteltext: Absatzweise gegen allowed/blocked wording pruefen. | future_documentation_only | Nach erstem BA-Draft eine paragraphenweise Wording-Review-Spezifikation schreiben. |
| agent_upgrade_04 | Table and figure package review | Nach Tabellen-/Figurenintegration: Caption, Artefakt und Limitation abgleichen. | future_documentation_only | Nach Tabellen-/Figurenintegration Caption-Checks gegen das kuratierte Paket spezifizieren. |
| agent_upgrade_05 | Advisor update summarisation | Nach Dozentenfeedback: Status und offene Entscheidungen knapp zusammenfassen. | future_documentation_only | Nach Dozentenfeedback eine bounded Meeting-Summary-Spezifikation schreiben. |
| agent_upgrade_06 | Monitor appendix review | Erst nach Human Review: Monitor-Appendix als Review-Workflow zusammenfassen. | future_documentation_only | Erst nach Human Review der Monitor-Cases Appendix-Summary-Spezifikation schreiben. |
| agent_upgrade_07 | Bounded MCP summary interface | Erst nach separatem Access-Goal: read-only Summary Interface spezifizieren. | future_deferred | Erst nach separatem Access-Goal ein read-only Summary-Interface mit Tests spezifizieren. |

## Guardrail Matrix

| upgrade_id | future_assistance_role | sequence_after_core_de | uses_current_core_sections | allowed_input_boundary | allowed_output_boundary | mandatory_audit_gate | blocked_actions_de | required_preconditions_de | current_status | next_safe_step_de |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| agent_upgrade_01 | Manual source review | Nach H1-H2-H3-Kern: fehlende Page-/Section-Notes und Claim-Support-Felder vorbereiten. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: source_id; evidence_id; required_check; source metadata; reviewer notes selected by a human; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: bounded checklist; missing-page-note warnings; no status changes; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with source_ids, evidence_ids, prompt hash, model, and output path. | Blockiert: promoting source status; inventing page numbers; citing candidate sources as thesis evidence. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with source_ids, evidence_ids, prompt hash, model, and output path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Nach manueller Quellenreview ein separates Prompt-/Audit-Design fuer fehlende Page Notes schreiben. |
| agent_upgrade_02 | Evidence-to-prose drafting | Nach stabiler Core-Section-CSV: Evidence-IDs in kurze Entwurfsnotizen uebersetzen. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: one selected evidence_id plus bounded linked artifact summaries; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: short draft note tied to the same evidence_id and primary_artifact; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with evidence_id, artifact versions, prompt hash, model, and output path. | Blockiert: calculating metrics; reading raw table dumps; adding claims outside allowed wording. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with evidence_id, artifact versions, prompt hash, model, and output path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Nach stabiler Traceability kurze evidence_id-bezogene Draft-Notes spezifizieren. |
| agent_upgrade_03 | Claim and wording review | Nach erstem Kapiteltext: Absatzweise gegen allowed/blocked wording pruefen. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: human-selected paragraph; linked evidence rows; chapter id; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: bounded overclaim warnings and safer wording suggestions; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with draft hash, evidence_ids checked, prompt hash, model, and warnings path. | Blockiert: relaxing blocked wording; making causal, private-information, profitability, or tradeability claims. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with draft hash, evidence_ids checked, prompt hash, model, and warnings path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Nach erstem BA-Draft eine paragraphenweise Wording-Review-Spezifikation schreiben. |
| agent_upgrade_04 | Table and figure package review | Nach Tabellen-/Figurenintegration: Caption, Artefakt und Limitation abgleichen. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: caption registry rows; selected draft caption text; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: missing-artifact, missing-limitation, or extra-raw-artifact warnings; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with package_ids, draft hash, prompt hash, model, and warnings path. | Blockiert: adding new tables or figures beyond the curated package without updating deterministic maps. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with package_ids, draft hash, prompt hash, model, and warnings path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Nach Tabellen-/Figurenintegration Caption-Checks gegen das kuratierte Paket spezifizieren. |
| agent_upgrade_05 | Advisor update summarisation | Nach Dozentenfeedback: Status und offene Entscheidungen knapp zusammenfassen. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: existing advisor report; consolidation docs; project status snapshot; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: meeting bullets, open questions, and next-step checklist; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with artifact paths, prompt hash, model, and summary path. | Blockiert: changing empirical claims; hiding unresolved source-review or Swiss-result gates. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with artifact paths, prompt hash, model, and summary path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Nach Dozentenfeedback eine bounded Meeting-Summary-Spezifikation schreiben. |
| agent_upgrade_06 | Monitor appendix review | Erst nach Human Review: Monitor-Appendix als Review-Workflow zusammenfassen. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: bounded case_id summaries; reviewed source notes; aggregate tier labels; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: appendix review summary and unresolved-evidence checklist; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und llm_audit_log entry with case_ids, artifact versions, prompt hash, model, and output path. | Blockiert: declaring misconduct; exposing wallet addresses by default; creating trading signals. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und llm_audit_log entry with case_ids, artifact versions, prompt hash, model, and output path. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_documentation_only | Erst nach Human Review der Monitor-Cases Appendix-Summary-Spezifikation schreiben. |
| agent_upgrade_07 | Bounded MCP summary interface | Erst nach separatem Access-Goal: read-only Summary Interface spezifizieren. | Ja, nur als bounded context: core_section_h1; core_section_h2; core_section_h3. Keine Rohartefakt-Dumps. | Nur bounded inputs/outputs: reviewed summary artifacts; explicit row limits; no raw SQL by default; max 50 rows by default; kein Rohdaten-Prompt. | Nur bounded inputs/outputs: bounded read-only summaries with row counts and artifact paths; max 50 rows by default; kein Rohdaten-Prompt. | Pflichtgate: separates genehmigtes Goal, Tests, bounded inputs und separate approved goal, access contract tests, and llm_audit_log integration. | Blockiert: raw table access; SELECT star; wallet-address exposure; order or trading paths. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, keine Rohdaten-Prompts, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. Zusaetzlich keine Runtime-Agenten, kein MCP, kein Model Routing, keine LLM-Metriken, kein Rohdaten-Prompt, keine Wallet-Adress-Exposition by default und keine Trading-Pfade. | Aktueller Agent-Gate-Status: deferred_future_work_only. Vor Aktivierung: Vor Aktivierung: separates genehmigtes Goal, bounded inputs, Tests, und separate approved goal, access contract tests, and llm_audit_log integration. Keine Nutzung im aktuellen Thesis-Kern. Zusaetzlich Source Review, Swiss Resultat-Gate und finaler QA-Status pruefen. | future_deferred | Erst nach separatem Access-Goal ein read-only Summary-Interface mit Tests spezifizieren. |

## Use Rule

Nutze diesen Plan erst nach dem H1-H2-H3-Draft als Future-Work-Gedanken. Vor Aktivierung braucht jede Rolle ein separates genehmigtes Goal, Tests, bounded inputs, Output-Limits und `llm_audit_log`. Bis dahin bleiben Runtime-Agenten, MCP, Model Routing, LLM-Metriken, Rohartefakt-Dumps, Wallet-Adress-Exposition by default und Trading-Pfade deaktiviert.

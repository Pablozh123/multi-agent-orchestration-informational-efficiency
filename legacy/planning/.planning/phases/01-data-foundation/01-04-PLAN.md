---
phase: 01-data-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
files_modified:
  - ingest/dune.py
  - data/market_maker_exclusions.json
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "whale_trades contains rows with amount_usd >= 10000"
    - "No row in whale_trades has a wallet_address that appears in market_maker_exclusions"
    - "All wallet_address values in whale_trades are lowercase"
    - "market_maker_exclusions table is populated with known Polymarket market-maker addresses"
    - "Running the ingest script twice does not produce duplicate rows (tx_hash UNIQUE constraint)"
  artifacts:
    - path: "ingest/dune.py"
      provides: "Dune Analytics API call and whale trade write with market-maker exclusion"
      exports:
        - filter_market_makers
        - ingest_dune
    - path: "data/market_maker_exclusions.json"
      provides: "Static list of known Polymarket market-maker wallet addresses"
  key_links:
    - from: "ingest/dune.py"
      to: "https://api.dune.com/api/v1/query/{query_id}/results"
      via: "httpx.get() with X-DUNE-API-KEY header from .env"
      pattern: "DUNE_API_KEY"
    - from: "ingest/dune.py"
      to: "data/thesis.db:whale_trades"
      via: "INSERT OR IGNORE with tx_hash UNIQUE constraint"
      pattern: "INSERT OR IGNORE INTO whale_trades"
    - from: "ingest/dune.py"
      to: "data/thesis.db:market_maker_exclusions"
      via: "Loaded on startup to build exclusion set for filter_market_makers()"
      pattern: "market_maker_exclusions"

user_setup:
  - service: dune_analytics
    why: "Whale trade data for Polymarket CLOB on Polygon blockchain"
    env_vars:
      - name: DUNE_API_KEY
        source: "Dune Analytics -> Settings -> API Key"
    dashboard_config:
      - task: "Fork or create a Dune query for Polymarket CLOB whale trades >= $10k in 2024"
        location: "https://dune.com/queries/new — reference existing: dune.com/rchen8/polymarket"
        note: "Develop and test query on Dune web UI before using API (zero credit cost on web)"
---

<objective>
Build ingest/dune.py to fetch large Polymarket trades (>=$10k) from Dune Analytics,
apply the market-maker exclusion filter, and write clean whale_trades rows.

Purpose: DATA-05 — Whale transactions with market-maker exclusion are required for
H3 (whale alpha) analysis in Phase 4.

Output: ingest/dune.py with filter_market_makers() (pure, testable) and ingest_dune()
(API call + write). A static JSON file of known market-maker addresses bootstraps the
exclusion list.
</objective>

<execution_context>
@C:/Users/chole/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chole/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@ingest/__init__.py
@requirements.txt

<interfaces>
<!-- Dune Analytics API (from RESEARCH.md): -->
<!--
GET https://api.dune.com/api/v1/query/{query_id}/results
Headers: {"X-DUNE-API-KEY": os.getenv("DUNE_API_KEY")}
Response: {"result": {"rows": [{...}, ...], "metadata": {...}}}

NOTE: PITFALL from STATE.md — develop and test SQL query on Dune web UI first.
Expected columns in query result (adapt to actual Dune CLOB table names):
  block_time (timestamp), tx_hash, taker (wallet), outcome_id/token_id,
  side/direction (BUY/SELL), amount_usd (trade size in USD), price (fill price)
-->

<!-- whale_trades table (correct schema from 01-01): -->
<!--
INSERT OR IGNORE INTO whale_trades
    (price_timestamp, tx_hash, wallet_address, market_id, direction, amount_usd, token_id, price_at_trade)
VALUES (?, ?, ?, ?, ?, ?, ?, ?)

wallet_address: ALWAYS lowercase (CLAUDE.md convention)
direction: must be 'BUY' or 'SELL' (CHECK constraint)
-->

<!-- market_maker_exclusions table: -->
<!--
INSERT OR IGNORE INTO market_maker_exclusions (wallet_address, label, source, added_at)
VALUES (?, ?, ?, ?)
-->

<!-- filter_market_makers interface (tested by test_ingest.py): -->
<!--
def filter_market_makers(rows: list[dict], exclusion_list: list[str]) -> list[dict]:
    Returns rows where row["wallet_address"].lower() not in {addr.lower() for addr in exclusion_list}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ingest/dune.py — filter_market_makers() and data/market_maker_exclusions.json</name>
  <files>ingest/dune.py, data/market_maker_exclusions.json</files>
  <behavior>
    - filter_market_makers(rows: list[dict], exclusion_list: list[str]) -> list[dict]:
      * Returns only rows where row["wallet_address"].lower() NOT in {addr.lower() for addr in exclusion_list}
      * Also normalizes row["wallet_address"] to lowercase in-place on the returned rows
      * This is the pure function tested by test_whale_trades_exclusion and test_whale_address_lowercase
    - data/market_maker_exclusions.json: JSON array of objects with keys: wallet_address (lowercase), label, source
      * Seed with known Polymarket market-maker addresses from RESEARCH.md sources
      * Minimum 3 entries (known Polymarket automated market makers)
      * Sources: Dune community dashboards (dune.com/rchen8/polymarket) list known MM addresses
    - ingest_dune(db_path: Path = DB_PATH) -> int:
      * Loads DUNE_API_KEY from .env via python-dotenv
      * If DUNE_API_KEY missing: raise EnvironmentError with clear message
      * Calls Dune API with the pre-configured query_id (constant in the file)
      * Maps response columns to whale_trades schema (handle column name variations from Dune)
      * Filters: amount_usd >= 10000.0 (whale threshold)
      * Calls filter_market_makers() with the exclusion list from market_maker_exclusions table
      * All wallet_address values: .lower() before any comparison or storage
      * direction normalization: map 'buy'/'BUY'/'Buy' all to 'BUY'; same for SELL
      * INSERT OR IGNORE into whale_trades
      * Also populates market_maker_exclusions table from data/market_maker_exclusions.json
      * Returns row count inserted
    - DUNE_QUERY_ID constant at module level — executor must set this to the actual query ID
      after developing the query on Dune web UI (see user_setup)
  </behavior>
  <action>
    Create data/market_maker_exclusions.json with at least 3 known Polymarket MM addresses.
    Research sources for known addresses:
      - Polymarket's own liquidity provision contracts
      - Known automated market makers visible in Dune community dashboards
    Format:
    [
      {"wallet_address": "0x...", "label": "Polymarket Official MM", "source": "dune.com/rchen8/polymarket"},
      ...
    ]
    All addresses must be lowercase hexadecimal.

    Create ingest/dune.py.
    Imports: httpx, json, pathlib, os, sqlite3, from dotenv import load_dotenv
    from ingest import get_connection, DB_PATH, to_utc_iso

    DUNE_QUERY_ID: int = 0  # REPLACE with actual query ID from Dune web UI
    WHALE_THRESHOLD_USD: float = 10_000.0
    EXCLUSIONS_PATH: Path = Path("data/market_maker_exclusions.json")

    Implement filter_market_makers() as pure function (no DB access).
    Implement load_exclusions(db_path: Path) -> list[str] that reads wallet_address from market_maker_exclusions table.
    Implement ingest_dune() with the pattern described above.

    if __name__ == "__main__": ingest_dune()
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_whale_trades_exclusion tests/test_ingest.py::test_whale_address_lowercase -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    test_whale_trades_exclusion passes: mock data with one MM address returns only the non-MM row.
    test_whale_address_lowercase passes: all wallet_address in result are lowercase.
    data/market_maker_exclusions.json exists with >= 3 entries.
    `python -c "from ingest.dune import filter_market_makers"` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    ingest/dune.py with Dune Analytics API integration. The script requires:
    1. DUNE_API_KEY in .env
    2. A Dune query developed and tested on the web UI
    3. DUNE_QUERY_ID constant updated in ingest/dune.py
  </what-built>
  <how-to-verify>
    PREREQUISITE: Complete user_setup steps first:
    1. Go to https://dune.com/settings/api and get your API key
    2. Add DUNE_API_KEY=your_key to .env
    3. Go to https://dune.com/queries/new and write a SQL query for Polymarket CLOB trades >= $10k in 2024
       Reference: https://dune.com/rchen8/polymarket for existing table names
       Example query structure:
         SELECT block_time, tx_hash, taker, outcome_id, 'BUY' as direction, size_usd
         FROM polymarket_polygon.clob_trades
         WHERE block_time BETWEEN TIMESTAMP '2024-01-01' AND TIMESTAMP '2024-11-05'
         AND size_usd >= 10000
    4. Run query on web UI, verify it returns rows. Note the query_id from the URL.
    5. Update DUNE_QUERY_ID in ingest/dune.py with the query_id from step 4.

    THEN run:
    6. .venv/Scripts/python.exe ingest/dune.py
    7. Expected: prints row count of whale trades inserted
    8. .venv/Scripts/python.exe -c "import sqlite3; c=sqlite3.connect('data/thesis.db'); print(c.execute('SELECT COUNT(*), MIN(amount_usd) FROM whale_trades').fetchone())"
    9. Expected: count > 0, min amount_usd >= 10000.0
    10. Cross-join check: no whale wallet in exclusion list:
        .venv/Scripts/python.exe -c "import sqlite3; c=sqlite3.connect('data/thesis.db'); print(c.execute('SELECT COUNT(*) FROM whale_trades wt JOIN market_maker_exclusions mme ON wt.wallet_address=mme.wallet_address').fetchone())"
        Must return (0,)
  </how-to-verify>
  <resume-signal>Type "approved" if whale_trades has rows and cross-join check returns 0. Type "blocked: {reason}" if Dune API or query setup failed.</resume-signal>
</task>

</tasks>

<verification>
Unit tests (no API needed):
  .venv/Scripts/python.exe -m pytest tests/test_ingest.py::test_whale_trades_exclusion tests/test_ingest.py::test_whale_address_lowercase -v

Post-ingest data integrity:
  .venv/Scripts/python.exe -c "
  import sqlite3; c = sqlite3.connect('data/thesis.db')
  print('Whale trades:', c.execute('SELECT COUNT(*) FROM whale_trades WHERE amount_usd >= 10000').fetchone())
  print('MM cross-join:', c.execute('SELECT COUNT(*) FROM whale_trades wt JOIN market_maker_exclusions mme ON wt.wallet_address=mme.wallet_address').fetchone())
  print('Lowercase check:', c.execute(\"SELECT COUNT(*) FROM whale_trades WHERE wallet_address != lower(wallet_address)\").fetchone())
  "
</verification>

<success_criteria>
- filter_market_makers() unit tests pass (no API key required)
- data/market_maker_exclusions.json exists with >= 3 lowercase addresses
- whale_trades contains rows with amount_usd >= 10000
- Zero wallet addresses in whale_trades that appear in market_maker_exclusions
- All wallet_address values are lowercase
- Running script twice does not increase row count (tx_hash UNIQUE constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-04-SUMMARY.md`
</output>
